<!DOCTYPE html>
<html>
  <head>
    <title>Song Index</title>
  </head>
  <body>
    <h1>Songs</h1>
    <%@songs.each.with_index do |song, i|%>
      <p><%= "#{i + 1}. "%><a href="/songs/<%=song.slug%>"><%=song.name%></a></p>
    <%end%>
  </body>
</html>
