<!DOCTYPE html>
<html>
  <head>
    <title>Specific Artist</title>
  </head>
  <body>
    <h2><%= @artist.name %></h2>
    <p>Songs: </p>
    <ul>
      <%@artist.songs.each do |song|%>
        <li><a href="/songs/<%= song.slug %>"><%= song.name %></a></li>
      <%end%>
    </ul>
  </body>
</html>
