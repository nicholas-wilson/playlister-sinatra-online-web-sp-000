<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Artists</title>
  </head>
  <body>
    <h1>Artists</h1>
    <%@songs.each.with_index do |song, i|%>
      <p><%= "#{i}. "%><a href="/songs/<%=song.slug%>"><%=song.name%></a></p>
    <%end%>
  </body>
</html>
