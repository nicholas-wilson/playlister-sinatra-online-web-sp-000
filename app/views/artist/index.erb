<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Artists</title>
  </head>
  <body>
    <h1>Artists</h1>
    <%@artists.each.with_index do |artist, i|%>
      <p><%= "#{i + 1}. "%><a href="/artists/<%=artist.slug%>"><%=artist.name%></a></p>
    <%end%>
  </body>
</html>
